document.addEventListener('DOMContentLoaded', function () {
  // When DOM is loaded, but possibly not all content.
}, false);

window.addEventListener('load', function () {
  // When all content is done loading.
}, false);